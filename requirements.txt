# ── 本地训练（Apple Silicon，MLX）──────────────────────────────
mlx-lm>=0.18.0           # LoRA fine-tuning on Mac M-series

# ── 云端训练（Kaggle / Colab A100，CUDA）─────────────────────
torch>=2.1.0
transformers>=4.40.0
peft>=0.10.0
datasets>=2.18.0
accelerate>=0.28.0
bitsandbytes>=0.43.0     # QLoRA，仅 CUDA 可用
trl>=0.8.0

# ── 实验追踪 ──────────────────────────────────────────────────
wandb>=0.16.0

# ── 评估 ──────────────────────────────────────────────────────
openai>=1.14.0
bert-score>=0.3.13
rouge-score>=0.1.2
pandas>=2.0.0

# ── 数据流水线 ────────────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
tqdm>=4.66.0

# ── 推理服务 ──────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0
sse-starlette>=2.0.0

# ── 工具 ──────────────────────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0
